# Configurações do Servidor
PORT=3001
NODE_ENV=development

# Configurações do Banco de Dados PostgreSQL
DATABASE_URL="postgresql://usuario:senha@localhost:5432/gerenciador_serfo"

# Configurações JWT
JWT_SECRET="seu-jwt-secret-super-secreto-aqui"
JWT_EXPIRES_IN="24h"

# Configurações de Email (para futuro)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="seu-email@gmail.com"
SMTP_PASS="sua-senha-de-app"
EMAIL_FROM="noreply@serfo.org"

# Configurações de Upload (para futuro)
UPLOAD_PATH="./uploads"
MAX_FILE_SIZE="5mb"

# Configurações de Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# URLs para CORS (separadas por vírgula)
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:5173"